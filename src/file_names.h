#ifndef INCLUDE_FILE_NAMES_H
#define INCLUDE_FILE_NAMES_H

#include <string>

namespace lwg {

  auto active_name()->std::string;
  auto closed_name()->std::string;
  auto defects_name()->std::string;
  auto old_toc_name()->std::string;
  auto toc_name()->std::string;
  auto status_index_name()->std::string;
  auto status_date_index_name()->std::string;
  auto section_index_name()->std::string;
  auto unresolved_toc_name()->std::string;
  auto unresolved_status_index_name()->std::string;
  auto unresolved_status_date_index_name()->std::string;
  auto unresolved_section_index_name()->std::string;
  auto unresolved_prioritized_name()->std::string;
  auto votable_toc_name()->std::string;
  auto votable_status_index_name()->std::string;
  auto votable_status_date_index_name()->std::string;
  auto votable_section_index_name()->std::string;
  auto open_index_name()->std::string;
  auto tentative_name()->std::string;
  auto unresolved_name()->std::string;
  auto immediate_name()->std::string;
  auto issues_for_editor_name()->std::string;

} // namespace lwg

#endif  // INCLUDE_FILE_NAMES_H
